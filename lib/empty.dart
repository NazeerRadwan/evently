import 'package:flutter/material.dart';

class empty extends StatelessWidget {
  const empty({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('This is an empty page')));
  }
}
